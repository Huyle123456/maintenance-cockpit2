<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:u="sap.ui.unified"
>
    <Dialog
        id="importOrdersDialog"
        title="{i18n>importOrdersDialogTitle}"
        icon="sap-icon://excel-attachment"
        contentWidth="680px"
        resizable="true"
        draggable="true"
    >
        <content>
            <VBox class="sapUiMediumMargin">
                <!-- Instruction & Mode Banner -->
                <MessageStrip
                    id="importOrdersBackendBanner"
                    text="{i18n>importOrdersBannerText}"
                    type="Information"
                    showIcon="true"
                    class="sapUiSmallMarginBottom"
                />

                <!-- File Selection Box -->
                <VBox class="sapUiSmallMarginBottom">
                    <Label
                        text="{i18n>importOrdersUploadLabel}"
                        design="Bold"
                        class="sapUiTinyMarginBottom"
                    />
                    <HBox alignItems="Center" width="100%">
                        <u:FileUploader
                            id="orderFileUploader"
                            name="file"
                            fileType="xlsx,xls,csv"
                            mimeType="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet,application/vnd.ms-excel,text/csv"
                            placeholder="{i18n>importOrdersUploadPlaceholder}"
                            change=".onImportFileChange"
                            width="400px"
                            buttonOnly="false"
                            buttonText="{i18n>importOrdersBrowseButton}"
                            icon="sap-icon://attachment"
                            class="sapUiSmallMarginEnd"
                        />
                        <Button
                            id="btnDownloadTemplate"
                            text="{i18n>importOrdersDownloadTemplate}"
                            icon="sap-icon://download"
                            type="Transparent"
                            press=".onDownloadImportTemplate"
                        />
                    </HBox>
                </VBox>

                <!-- Selected File Info Card -->
                <Panel
                    id="pnlSelectedFileInfo"
                    headerText="{i18n>importOrdersSelectedSummary}"
                    expandable="false"
                    expanded="true"
                    visible="{= !!${importModel>/fileName} }"
                    class="sapUiNoContentPadding sapUiSmallMarginBottom"
                >
                    <VBox class="sapUiSmallMargin">
                        <HBox alignItems="Center" class="sapUiTinyMarginBottom">
                            <Label text="{i18n>importOrdersFileName}" width="120px" design="Bold" />
                            <Text text="{importModel>/fileName}" />
                        </HBox>
                        <HBox alignItems="Center" class="sapUiTinyMarginBottom">
                            <Label text="{i18n>importOrdersFileSize}" width="120px" design="Bold" />
                            <Text text="{importModel>/fileSize}" />
                        </HBox>
                        <HBox alignItems="Center">
                            <Label text="{i18n>importOrdersStatus}" width="120px" design="Bold" />
                            <ObjectStatus
                                text="{importModel>/statusText}"
                                state="{importModel>/statusState}"
                                inverted="true"
                            />
                        </HBox>
                    </VBox>
                </Panel>

                <!-- Dynamic Status Message Strip -->
                <MessageStrip
                    id="importOrdersMessageStrip"
                    text="{importModel>/statusMessage}"
                    type="{importModel>/statusType}"
                    showIcon="true"
                    showCloseButton="false"
                    visible="{= !!${importModel>/statusMessage} }"
                    class="sapUiSmallMarginTop"
                />

                <!-- Progress Indicator for Background Async Processing -->
                <VBox visible="{= !!${importModel>/isProcessing} }" class="sapUiSmallMarginTop">
                    <HBox justifyContent="SpaceBetween" alignItems="Center" class="sapUiTinyMarginBottom">
                        <Label text="{i18n>importOrdersProgressLabel}" design="Bold" />
                        <Text text="{importModel>/progressPercent}%" class="sapUiTinyMarginEnd" />
                    </HBox>
                    <ProgressIndicator
                        id="importProgressIndicator"
                        percentValue="{importModel>/progressPercent}"
                        displayValue="{importModel>/progressPercent}%"
                        state="{importModel>/progressState}"
                        showValue="true"
                        height="1.5rem"
                    />
                </VBox>

                <!-- Error Notice and Download Button if previous import had errors -->
                <VBox visible="{= !!${importModel>/hasErrors} }" class="sapUiSmallMarginTop">
                    <MessageStrip
                        text="{i18n>importOrdersRecentErrorsNotice}"
                        type="Warning"
                        showIcon="true"
                        class="sapUiTinyMarginBottom"
                    />
                    <Button
                        id="btnDownloadLastErrors"
                        text="{i18n>importOrdersRecentErrorsBtn}"
                        icon="sap-icon://excel-attachment"
                        type="Reject"
                        press=".onDownloadErrorRowsFile"
                    />
                </VBox>
            </VBox>
        </content>
        <beginButton>
            <Button
                id="btnConfirmImport"
                text="{= ${importModel>/isProcessing} ? ${i18n>importOrdersBtnProcessing} : ${i18n>importOrdersBtnUpload} }"
                type="Emphasized"
                icon="{= ${importModel>/isProcessing} ? 'sap-icon://synchronize' : 'sap-icon://upload' }"
                enabled="{= ${importModel>/canImport} &amp;&amp; !${importModel>/isProcessing} }"
                press=".onConfirmImportOrders"
            />
        </beginButton>
        <endButton>
            <Button
                id="btnCancelImport"
                text="{= ${importModel>/isProcessing} ? ${i18n>importOrdersBtnCloseBackground} : ${i18n>commonCancel} }"
                press=".onCancelImportOrders"
            />
        </endButton>
    </Dialog>
</core:FragmentDefinition>
